# M8.1 — Leave Module Follow-ups

**Branch:** `m8.1-leave-followups`
**Predecessor:** M8 — closed 2026-06-09, merged at e9851a0, tag `m8-close`
**Status:** In progress

## Scope

Five items deferred from M8, sharing leave-balance scope:

| Item | Description |
|---|---|
| TD-037 | Notification silent no-op: zero notification rows after submit→approve at M8 gate |
| TD-036 | CL annual credit job missing: all CL balances start at zero |
| E-016 | Legacy leave balance import (CSV + per-employee form) |
| E-017 | Withdraw approved leave with balance reversal + notifications |
| E-022 | Admin manual edit of LeaveBalance and LeaveRequest records |

## Phase log

| Phase | Description | Status |
|---|---|---|
| 1 | TD-037 diagnostic + fix | In progress |
| 2 | TD-036 CL annual credit job | TODO |
| 3 | E-016 service + CSV parser | TODO |
| 4 | E-016 admin page UI | TODO |
| 5 | E-017 service extension + notifications | TODO |
| 6 | E-017 UI (withdraw modal) | TODO |
| 7 | E-022 LeaveBalance edit page | TODO |
| 8 | E-022 LeaveRequest edit + post-facto apply | TODO |
| 9 | Gate | TODO |

## Gate checklist

### Non-E2E suite (floor ≥ 1196)
- [ ] `uv run pytest tests/unit/ tests/integration/ -q --no-cov` — all pass, count ≥ 1196

### E2E suite (×3 consecutive runs)
- [ ] Run 1 — all pass
- [ ] Run 2 — all pass (same session, no `docker compose down -v`)
- [ ] Run 3 — all pass

### Migration round-trip (test DB only)
- [ ] `uv run alembic downgrade base` on test DB — no errors
- [ ] `uv run alembic upgrade head` — no errors

### Fresh-clone ritual
- [ ] Clone, `uv sync`, `docker compose up -d`, `uv run alembic upgrade head`, `uv run python scripts/seed.py` — all pass
- [ ] Full test suite passes on fresh clone

### Notification smoke check
- [ ] After submit→approve cycle: `SELECT COUNT(*) FROM notifications;` ≥ 1 (TD-037 fixed)

### Manual walkthroughs (G–M)
- [ ] G: submit leave → approve → requestor sees approved in history + notification row created
- [ ] H: withdraw approved leave → balance reversed
- [ ] I: import balance CSV → employee balance updated
- [ ] J: admin edit balance row → audit row visible
- [ ] K: admin edit request state → state updated
- [ ] L: post-facto leave apply → "Post-facto application" badge visible
- [ ] M: credit policy config edit → entitlement updated

### Incognito check (each new page)
- [ ] `/admin/leave/balance-import` — no chrome before redirect
- [ ] `/admin/leave/balance-edit` — no chrome before redirect
- [ ] `/admin/leave/request-edit` — no chrome before redirect
- [ ] `/admin/leave/credit-policy` — no chrome before redirect

### Docs sweep
- [ ] `docs/rfp_errata.md` — E-016/E-017/E-022 marked Resolved
- [ ] `docs/tech_debt.md` — TD-036/TD-037 moved to Resolved; TD-038/TD-039/TD-040 filed
- [ ] `docs/coverage_matrix.md` — M8.1 row appended
- [ ] `docs/runbook.md` — M8.1 ops section complete
- [ ] `docs/ux_charter.md` — §15 appended
- [ ] `docs/prompts/gate_verification.md` — M8.1 lessons appended
- [ ] `CLAUDE.md` — both "Current milestone" lines updated

## Resume notes

**Phase 1 (TD-037) notes:**
- Root cause confirmed as H1: auto-approve path in `ApprovalRequestService.submit()` (lines 108–112) calls `_run_post_approval()` but never calls `_enqueue_notifications(action="approve")` for the requestor.
- Fix: add `_enqueue_notifications` call inside the `if request.current_stage > len(channel):` block, after `_run_post_approval()`, mirroring the pattern at line 255 in `approve()`.
- Evidence: `test_auto_approve_creates_requestor_notification` reproduces the bug (0 notifications) before the fix.
- M8 gate walkthrough used `vc_user` as sole holder of all SCL channel roles → every walkthrough triggered auto-approve → zero notification rows.

**Next phase (Phase 2 — TD-036):**
- Add `LeaveCreditPolicy` model + migration
- Add `leave_credit_runs` sidecar table + migration
- Add `credit_annual_cl` Celery beat task (Jan 1, 03:00 UTC)
- Admin config page at `/admin/leave/credit-policy`
