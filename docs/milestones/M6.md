# Milestone M6 — Audit UI

## Status
Not started.

## Scope
M6 builds the audit-trail viewing UI on top of the audit_log table that has been silently accumulating entries since M2. Specifically:

(Placeholder — to be filled when M6 planning starts. Likely items:)
- /admin/audit page with filterable, paginated audit-log view (filters: actor, resource, action, date range, scope)
- Per-resource audit views accessible from within config pages (e.g. "view audit history for this counsellor")
- Export of audit views (CSV minimum, DOCX-on-letterhead if institutional)
- Permission gating: audit:read scope — SysAdmin + Registrar family see all; HoD family sees their dept-scoped audit; Director sees campus-scoped audit
- Performance: pagination + indexed queries since audit_log will be the largest table

## Authority files
(References — populate during planning)
- docs/durgam_rfp_v3.pdf
- docs/durgam_informal_requirements.docx
- docs/rfp_errata.md
- docs/coverage_matrix.md
- docs/modules/audit.md (create when designing)

## Forward concerns from prior milestones to address (or defer with reasoning)
- Calendar entry conflict detection (deferred from M5b)
- Per-user vs role-inbox notification preferences (informational from M5b Z1)

## Gate verification protocol
Follow docs/prompts/gate_verification.md. Specifically per M5b lessons:
- Manual ritual non-negotiable, multiple rounds expected
- Every PASS claim must include evidence (file:line OR end-to-end test name)
- Multi-step flows need integration tests through state handlers
- Spec divergences flagged in build reports, not silent
- Permission grants verified against inheritance chain
- "Pre-existing" diagnostics require schema-matched baseline

## Test count baseline
M5b close-out: ~734 passing, 3x deterministic against fresh DB.
