# M10 — Faculty Module

**RFP reference:** §9.7 (Faculty Module spec), §8.3 (Faculty Listing), §10.2 (Faculty dashboard), §12 (M10 gate). Informal requirements `docs/durgam_informal_requirements.docx` — Faculty Module section (profile + 11 request types).
**Branch:** `m10-faculty`
**Predecessor:** M9 — closed 2026-06-14, merged at `2d819c4`, tag `m9-close` on `afc1f4b`. Post-merge docs commit `7842eb9`. Branch forks from main at the post-merge state.
**Status:** In progress. All 20 design decisions frozen (2026-06-14). Phase 0 orient ready.

**RFP corrections (binding):**

1. **E-019 absorbed** — Campus-scoped Director routing implemented via `Faculty.campus_id`; leave matrix engine and general approval engine consult Faculty record at routing time.
2. **E-021 absorbed** — HoD recommend-via stage required for FACULTY leave when applicant designation is not Prof/AssocProf/SrProf and applicant has a Faculty record. Prof-tier opt-in HoD prefix via submit-form checkbox. AhoD fallback per Q4a campus-aware rule; AhoD-when-HoD-vacant fallback policy deferred (document gap at M10 close).
3. **TD-038 absorbed** — Withdrawal notification recipients filtered by campus-dept scope using Faculty record.
4. **TD-039 absorbed** — Leave admin pages enforce DIRECTOR-tier campus scope via `Faculty.campus_id` of the user being administered.
5. **E-007 R1 partial absorption** — Runtime approver-choice at OR-set channel stages shipped via M7 engine extension. Committee-member rank-preference UI (R2) remains M14.
6. **TD-066, TD-067** — Composer-scope label storage and module-private convention cleanup folded into Phase 13 docs sweep.
7. **TD-068 resolved** — `scripts/seed.py` user-creation idempotency fixed at Phase 1; regression test verifies double-run yields identical user count.

**M7 engine extension scope:** This milestone extends the M7 generic approval engine to support runtime approver-choice forks (OR-set stages), submit-time requestor-choice picker, and a conditional first-stage resolver `dept_head_at_requestor_campus`. The 4 existing ApprovalProcess seeds (`CPC_FUND_RELEASE`, `NRF_APPROVAL`, `DSW_CLEARANCE`, `LEAVE_APPROVAL`) migrate to the new structured channel shape with single-role stages, preserving current behaviour. All existing approval-request and leave-request tests stay green.

**Faculty vs NonRegularFaculty:** `Faculty` (this milestone) is 1:1 with `User` and represents regular-teaching staff (`employee_type='regular_teaching'`). `NonRegularFaculty` (E-003 / M5b) is inline-stored external personnel without User accounts. The two coexist; course allocation (M13) pulls from both pools.

---

## Configuration governance

| Surface | Configured by | Rationale |
|---------|---------------|-----------|
| Faculty profile (self-edit fields) | Faculty themselves (owner) | RFP §9.7 explicit — "each faculty manages his page/pages/module" |
| Faculty profile (admin-locked fields) | REGISTRAR + REGISTRAR_OFFICE + HR_HEAD | Personnel-record integrity (employee_id, designation, joining_date, dept/campus, employee_type) |
| Faculty bulk import | REGISTRAR + REGISTRAR_OFFICE + HR_HEAD | Operational scale; mirrors M5b user import permission |
| Designation taxonomy additions | SYSTEM_ADMIN | Vocabulary-level decision; affects rule engines |
| ApprovalProcess channel structures (10 FacultyRequest types + existing 4) | SYSTEM_ADMIN | Existing M7 ownership; extended for OR-set stages |
| Sensitive read (Aadhaar, PAN, uploaded documents) | REGISTRAR + REGISTRAR_OFFICE + IQAC_COORDINATOR + IQAC_OFFICE | RFP §9.7 explicit — "Registrar and his office and IQAC coordinator and his office can view all the above information" |
| Faculty deactivation | REGISTRAR + HR_HEAD | Lifecycle event; soft-delete via TimestampedSoftDelete |

**Principle:** Faculty owns their record; Registrar-tier owns personnel-integrity fields; HR_HEAD shares write on admin-locked fields. Sensitive PII view restricted to Registrar + IQAC tiers per RFP. SYSTEM_ADMIN retains god-role override.

---

## Scope

| Item | Description |
|------|-------------|
| D-001 | `Faculty` model + Alembic migration. 1:1 with User via `user_id` unique FK. Fields per RFP Listing 8.3 + informal req: employee_id, title, first/middle/last_name, designation (FK to Designation), department_id, campus_id, joining_date, is_vacation_employee, phone, whatsapp, alt_phone, alt_email, photo_file_id, emergency_contact_{name,relation,phone}, is_phd + 4 PhD fields, orcid, linkedin, google_scholar, researchgate. Gender remains on User (already there for M8 ML eligibility). PAN/Aadhaar remain on User (already there as `pan_enc`/`aadhaar_enc`). |
| D-002 | `FacultyEducation`, `FacultyExperience`, `FacultyExpertise` multi-row sub-models + migrations |
| D-003 | `FacultyDocument` model + migration. Uses FileAsset with `purpose="faculty_document"`, `metadata_json={"faculty_id": uuid, "doc_type": "degree_certificate|phd_certificate|other"}`; M9 announcement-attachment pattern; ≤2MB PDF only |
| D-004 | `FacultyWorkload` model + migration. Manual entry at M10; propagation hooks reserved for M13 |
| D-005 | `FacultyRequest` parametric model + migration. `request_type` enum (10 values) + `payload_json` + `approval_request_id` FK (mirrors LeaveRequest↔ApprovalRequest pattern from M8) |
| D-006 | Designation taxonomy expansion: existing 4 rows → 7 rows (`asst_prof_l10`, `asst_prof_l11`, `asst_prof_l12`, `assoc_prof`, `prof`, `sr_prof`, `instructor`). Data migration preserves existing FK references in PurchaseCommitteeTemplate.eligible_designations and any other consumers. |
| D-007 | **M7 engine Part A**: `ApprovalProcess.channel_definition_json` migration (structured stages: each = single role OR OR-set + chooser). Back-compatible with existing `channel_role_codes` (read-only fallback). Admin UI rewrite at `/admin/config/approval-processes` exposes the structured editor. |
| D-008 | **M7 engine Part B**: runtime fork resolution. Approver decision adds a "next-approver" picker when the next stage is an OR-set with `chooser="approver"`. Submit-time picker for OR-sets with `chooser="requestor"`. Conditional first-stage resolver `dept_head_at_requestor_campus` (HoD-at-campus → AhoD-at-campus → requestor-choice). |
| D-009 | 10 ApprovalProcess seeds with structured channels: `FDP_REQUEST`, `CONFERENCE_REQUEST`, `INVITED_TALK_REQUEST`, `PROFESSIONAL_MEMBERSHIP_REQUEST`, `WFH_REQUEST`, `FIELD_VISIT_REQUEST`, `APC_REQUEST`, `TRAVEL_REQUEST`, `INHOUSE_RESEARCH_FUNDING_REQUEST`, `EXTERNAL_GRANT_PROPOSAL_REQUEST`. (Existing `CPC_FUND_RELEASE` covers fund-release.) Default channels documented in Specifications section below; sys_admin can adjust live. |
| D-010 | Permission triples: `faculty:read:*`, `faculty:write:own`, `faculty:write:admin` (Registrar/HR_HEAD), `faculty_sensitive:read:*` (Registrar+IQAC), `faculty_document:read:*` (Registrar+IQAC), `faculty_request:create:own`, `faculty_request:read:own`, `faculty_request:read:*` (Registrar audit), `faculty_workload:read:*`, `faculty_workload:write:own`, `faculty:bulk_import:*` (Registrar+HR_HEAD), `designation:read:*`, `designation:configure:*` (SYSTEM_ADMIN), `approval_process:configure:*` (SYSTEM_ADMIN, extended for structured channels) |
| D-011 | FACULTY role permission delta: add `faculty:write:own`, `faculty_request:create:own`, `faculty_workload:write:own`, `faculty_request:read:own`, `faculty:read:*` (all faculty visible to all). PROFESSOR / ASSOC_PROFESSOR inherit. HOD/AHOD inherit + `faculty_request:read:*` for dept scope (handled at handler-body fine-grained check per M3 pattern). |
| D-012 | FileAsset MIME whitelist extension for `purpose="faculty_photo"` (image/jpeg + image/png; 1MB cap). Existing PDF whitelist for other purposes unchanged. |
| D-013 | Faculty self-service profile page `/faculty/profile` — multi-section UI: Identity (read-only summary) / Contact / Photo / External IDs / Education / Experience / Expertise / Documents / Sensitive (PAN, Aadhaar — own edit only) |
| D-014 | Faculty admin page `/admin/faculty` — Registrar + HR_HEAD tier: view all (incl. sensitive), edit admin-locked fields, deactivate (soft-delete), restore |
| D-015 | Faculty browse page `/faculty` — directory view for all authenticated users (excludes sensitive fields per §9.7 visibility rule); searchable, filterable by dept/campus/designation |
| D-016 | Faculty bulk import `/admin/faculty/import` — two-stage validate→commit, M5b pattern. CSV columns per Q11. |
| D-017 | Faculty requests page `/faculty/requests` — two tabs: "My requests" (raised by me, all states) + "For my decision" (pending if I am an approver in any stage). Integrates with M7 approval inbox (Approvals link continues to surface; this page is a faculty-context overlay). |
| D-018 | Campus-scoped Director routing (E-019): leave matrix engine + general approval engine consult `Faculty.campus_id` when matrix `scope_type='campus'`. Falls back gracefully when requestor has no Faculty record (e.g., non-teaching staff). |
| D-019 | HoD recommend-via stage for FACULTY leave (E-021): matrix YAML rewrite. HoD insertion for designations ∈ {AsstProf L10/L11/L12, Lecturer, Lecturer-Sr, Lecturer-SG} + employee_type ∈ {honorary_teaching, superannuated_teaching, visiting_fellow}. Prof-tier (Prof/AssocProf/SrProf) opt-in checkbox at submit ("Send through my HoD for recommendation"). |
| D-020 | Placeholder backfill migration (5 tables): `faculty_mentor_assignments`, `class_teacher_assignments`, `class_coordinator_assignments`, `non_owned_courses`, `ug_timetable`. Each gets nullable `faculty_id` FK → data migrated from seed → `faculty_id_placeholder` column dropped → `faculty_id` tightened to NOT NULL where applicable. |
| D-021 | Faculty pickers in M5b assignment pages: replace free-text faculty inputs with Faculty dropdown sourced from `/api/faculty/picker` endpoint (filtered by dept/campus where relevant). |
| D-022 | NonRegularFaculty minimal expansion: add `contract_end_date` (or rename `available_to` for semantic clarity — confirm at Phase 9), `renewal_count int default 0`, `latest_contract_file_id UUID nullable FK to fileasset` |
| D-023 | TD-068 seed.py user-creation idempotency resolved at Phase 1: audit all user-create paths, add upsert-by-username (`on_conflict_do_nothing` on username unique constraint), regression test seeds twice on fresh DB and asserts identical user count |
| D-024 | TD-038, TD-039 leave admin campus-scope enforcement via Faculty.campus_id |
| D-025 | Faculty dashboard widget — 3 tiles per §10.2 partial: my pending leave (M8), my pending approval requests (M7), my workload summary tile. Tiles requiring courses/publications/mentees blocked downstream. |
| D-026 | Audit emissions: service-layer direct `write_audit_row` for FacultyRequest lifecycle (M9 announcement pattern); `@audit_action` state-decorator for Faculty CRUD (M3 config pattern); audit row for OR-set picks records the chosen role at each stage |

---

## Out of scope (deferred)

| Item | Target milestone |
|------|-----------------|
| Faculty research sub-models (Publications, Patent, Awards, Collaboration, ResearchProject, Consultancy, EContent, PhDGuidance, ResearchGuidance) + DOI/CrossRef lookup | M11 |
| Class teacher / mentor / coordinator rich write UI with Faculty dropdowns | M14 (read-only Faculty picker shipped at M10 Phase 11) |
| Course allocation workflow + propagation into FacultyWorkload | M13 |
| Faculty workload merged DOCX export across campuses | M13/M14 |
| Dual-role calendar ownership "Creating as" role selector | M14 |
| Faculty dashboard widgets requiring research/courses/mentees data | M11/M13/M14 |
| Purchase committee rank-preference enforcement UI (E-007 R2) | M14 |
| Faculty event notifications (email/in-app) | Future M-Notifications |
| AhoD-when-HoD-vacant fallback policy (matrix-level fallback semantics) | Future |
| Joint-appointment / cross-department Faculty modelling | Future (1:1 strict at M10) |
| Faculty publication-DOI tile on dashboard | M11 |
| Faculty bulk import for non-regular staff (visiting/honorary) | Future (NonRegularFaculty admin UI only) |

---

## Frozen design decisions

Q1–Q20 resolved on 2026-06-14.

| Q | Resolution | One-line rationale |
|---|------------|--------------------|
| Q1 | Faculty 1:1 with User via `user_id` unique FK | RFP Listing 8.3 explicit; joint appointments deferred |
| Q2 | Faculty record auto-created iff `User.employee_type='regular_teaching'` | Data-driven; aligns with leave-rules vacation-employee semantics |
| Q3 | FacultyRequest single parametric model: `request_type` + `payload_json` + `approval_request_id` FK | Mirrors M8 LeaveRequest↔ApprovalRequest; one service per type for validation |
| Q4 | ApprovalProcess channel = ordered stages; each stage = single role OR OR-set with `chooser` ∈ {approver, requestor}; first stage MAY use conditional resolver `dept_head_at_requestor_campus` | E-007 R1 absorbed; configurable per process; sys_admin owns |
| Q4a | HoD/AhoD remain department-scoped UserRoles; physical campus derived from holder's `Faculty.campus_id` | Avoids UserRole schema change; cleanest data model |
| Q4b | OR-set chooser is per-stage config: approver-side at AhoD and HoD forks; requestor-side at no-dept-head and at Prof-tier opt-in | Matches institutional decision authority |
| Q4c | (a) AhoD → HoD/Director: approver-side. (b) HoD → Director/Dean-of-school: approver-side. (c) No HoD and no AhoD at faculty's campus: requestor-side at submit. (d) Prof/AssocProf/SrProf leave opt-in HoD prefix: requestor-side checkbox at submit. | Locked Bala 2026-06-14 |
| Q5 | Designation expansion: 7 rows — asst_prof_l10, asst_prof_l11, asst_prof_l12, assoc_prof, prof, sr_prof, instructor | RFP Listing 8.3 explicit |
| Q6 | Faculty self-edit: contact, photo, external IDs, education/experience/expertise (full CRUD), documents. Admin-set: employee_id, designation, joining_date, department_id, campus_id, employee_type, is_vacation_employee, gender. Sensitive (self-edit, Registrar/IQAC read): PAN, Aadhaar | Personnel-record integrity vs self-service balance |
| Q7 | FileAsset `purpose="faculty_photo"`, MIME whitelist extended to image/jpeg + image/png for that purpose only; 1MB cap | Minimal extension; isolated to one purpose |
| Q8 | FileAsset `purpose="faculty_document"` + `metadata_json={"faculty_id", "doc_type"}`; ≤2MB PDF; M9 announcement-attachment pattern | Proven pattern; no schema addition |
| Q9 | FacultyWorkload model + manual entry UI at M10; propagation hooks reserved for M13 | Decouples from M13 dependencies |
| Q10 | Placeholder backfill: per-table migration sequence: ALTER ADD nullable `faculty_id` FK → data migration from seed → DROP `faculty_id_placeholder` → ALTER `faculty_id` NOT NULL where applicable. Each table migrated independently; rollback verified | Safe per-step rollback |
| Q11 | Bulk CSV import mandatory cols: employee_id, username, first_name, last_name, designation_code, dept_code, campus_code, joining_date, gender. Optional: middle_name, title, phone, whatsapp, alt_phone, alt_email, emergency_contact_{name,relation,phone}, is_phd, phd_thesis_title, phd_registration_number, phd_awarding_institution, phd_year, orcid, linkedin, google_scholar, researchgate. M5b two-stage validate→commit | Operational pragmatism |
| Q12 | E-019 full implementation; backfill ensures all existing Director-routed seed users have Faculty records with correct campus_id | Unblocks scope routing |
| Q13 | HoD recommend-stage required for non-Prof tier (incl. honorary/superannuated/visiting_fellow). Prof/AssocProf/SrProf opt-in submit-form checkbox prepends HoD stage. AhoD-when-HoD-vacant: HoD-only at M10; matrix-level fallback deferred | Bala clarification 2026-06-14 |
| Q14 | Profile completion derived at runtime from required-field presence (Faculty record exists + designation set + dept + campus + joining_date + contact phone + emergency contact populated); no schema flag | Avoids stale-flag bug class |
| Q15 | TD-068 seed.py idempotency resolved at M10 Phase 1; regression test in `tests/integration/test_seed_idempotency.py` | Faculty seed makes this a critical path |
| Q16 | NonRegularFaculty expansion: `contract_end_date` (resolved at Phase 9 — rename or add), `renewal_count`, `latest_contract_file_id`. Full HR workflow deferred | Operational pragmatism; minimal disruption |
| Q17 | Faculty dashboard widget: 3 tiles (pending leave, pending approvals, workload summary). Other §10.2 Faculty tiles blocked downstream | YAGNI at M10 |
| Q18 | Audit: service-layer `write_audit_row` for FacultyRequest lifecycle; `@audit_action` for Faculty CRUD; OR-set picks audited per stage with chosen role | Combined M3+M7+M9 patterns |
| Q19 | TD-066, TD-067 cleanup folded into Phase 13 docs sweep | Cheap, non-blocking |
| Q20 | Faculty-event notification emails deferred to Future M-Notifications | Aligned with M9 Q15 deferral |

---

## Specifications

### 10 ApprovalProcess seeds — default channel structures

Each channel below uses the structured `channel_definition_json` shape introduced by D-007. The first stage uses the conditional resolver `dept_head_at_requestor_campus`. Subsequent stages are either single roles or OR-sets.

| Process code | Title | Default channel (after first stage) | Final approver |
|--------------|-------|-------------------------------------|----------------|
| `FDP_REQUEST` | Faculty Development Programme | dept_head → {DIRECTOR, DEAN}:approver → VC | VC |
| `CONFERENCE_REQUEST` | Conference / Symposium / Seminar / Workshop | dept_head → {DIRECTOR, DEAN}:approver → VC | VC |
| `INVITED_TALK_REQUEST` | Invited Talk | dept_head → DIRECTOR | DIRECTOR |
| `PROFESSIONAL_MEMBERSHIP_REQUEST` | Professional Body Membership | dept_head → DIRECTOR | DIRECTOR |
| `WFH_REQUEST` | Work From Home | dept_head → DIRECTOR | DIRECTOR |
| `FIELD_VISIT_REQUEST` | Field / Industry Visit | dept_head → DIRECTOR | DIRECTOR |
| `APC_REQUEST` | Article Processing Charge | dept_head → FINANCE_OFFICER → VC | VC |
| `TRAVEL_REQUEST` | Travel (award/examiner/visiting/committee) | dept_head → DIRECTOR → VC | VC |
| `INHOUSE_RESEARCH_FUNDING_REQUEST` | In-house Research Funding | dept_head → {DEAN, DIRECTOR}:approver → REGISTRAR → VC | VC |
| `EXTERNAL_GRANT_PROPOSAL_REQUEST` | External Grant Proposal Submission | dept_head → REGISTRAR | REGISTRAR |

These are seed defaults; sys_admin can adjust live via the admin UI.

### `channel_definition_json` shape

```json
{
  "version": 1,
  "stages": [
    {"resolver": "dept_head_at_requestor_campus"},
    {"role_codes": ["DIRECTOR", "DEAN"], "chooser": "approver"},
    {"role_codes": ["VC"]}
  ]
}
```

Field semantics:
- `resolver` — named first-stage resolver; takes precedence over `role_codes`. Resolves to a tuple (resolved_role_code, resolved_user_id) OR raises `RequiresRequestorChoice` with candidate list.
- `role_codes` — single-element list means single-role stage; multi-element list means OR-set.
- `chooser` — `"approver"` (previous approver picks at decide-and-forward) or `"requestor"` (requestor picks at submit). Required when `len(role_codes) > 1`. Absent for single-role stages.

### Conditional resolver `dept_head_at_requestor_campus`

```
1. Resolve requestor's Faculty record. If absent, raise NotFacultyError.
2. Find HoD: UserRole where role_code='HOD' AND scope_id=Faculty.department_id (one row max per dept).
3. If HoD exists and HoD's Faculty.campus_id == requestor's Faculty.campus_id:
   → return ("HOD", hod_user_id)
4. Else, find AhoD at faculty's campus: UserRole where role_code='AHOD' AND scope_id=Faculty.department_id,
   join through user_id to Faculty where campus_id = requestor's Faculty.campus_id.
   If found → return ("AHOD", ahod_user_id)
5. Else: raise RequiresRequestorChoice(candidates=[
     ("HOD", hod_user_id_if_any),
     ("DIRECTOR", director_at_faculty_campus_user_id)
   ])
```

Submit flow catches `RequiresRequestorChoice` and presents the picker. Selected (role, user) is recorded as stage 1's resolved value in `ApprovalRequest.resolved_channel_json`.

### Designation taxonomy expansion — data migration plan

Current 4 rows: `senior_professor`, `professor`, `associate_professor`, `assistant_professor` (see M5b/E-007 seed).

New 7 rows (final state): `asst_prof_l10`, `asst_prof_l11`, `asst_prof_l12`, `assoc_prof`, `prof`, `sr_prof`, `instructor`.

Mapping:
- `senior_professor` → `sr_prof`
- `professor` → `prof`
- `associate_professor` → `assoc_prof`
- `assistant_professor` → split into 3: `asst_prof_l10`, `asst_prof_l11`, `asst_prof_l12`. Existing FK references default to `asst_prof_l10` (lowest level; conservative). Document this default in migration commit message.
- New: `instructor`

Data migration steps:
1. INSERT new 7 rows with new codes.
2. UPDATE `PurchaseCommitteeTemplate.eligible_designations` (text array column) to remap old codes to new codes.
3. UPDATE any other consumers (audit at migration write time).
4. UPDATE existing seed users' Designation FK references.
5. DELETE (or soft-delete) the 4 old rows ONLY after FK references confirmed remapped.

### Placeholder backfill migration plan (D-020) — per-table sequence

For each of `faculty_mentor_assignments`, `class_teacher_assignments`, `class_coordinator_assignments`, `non_owned_courses`, `ug_timetable`:

1. ALTER ADD `faculty_id UUID NULL` + FK to `faculties.id` + ix index.
2. UPDATE rows: set `faculty_id` from a lookup table populated by Phase 1 seed (which Faculty row matches the placeholder string). For seeded test rows, the seed-script update is deterministic.
3. ALTER `faculty_id` NOT NULL.
4. ALTER DROP `faculty_id_placeholder`.

Per-table commit + migration; never bundled into one migration. Rollback path tested at Phase 11.

### Seed user backfill (Phase 1)

Audit list — these existing seeded users get a Faculty record at Phase 1 (provided `employee_type='regular_teaching'` is set):

- `dean_sci` — designation `prof`, dept TBD per Phase 1 self-audit, campus PRASANTHI
- `hod_dmacs` — designation `prof`, dept DMACS, campus PRASANTHI
- `ahod_dmacs` — designation `assoc_prof`, dept DMACS, campus TBD
- `hod_office_dmacs` — NOT a faculty (administrative office role) — confirm employee_type='regular_non_teaching', no Faculty record
- `faculty_user` — designation `asst_prof_l10`, dept DMACS, campus PRASANTHI
- `professor_user` (if seeded by M8) — designation `prof`
- `assoc_professor_user` (if seeded by M8) — designation `assoc_prof`
- `student_001` — no Faculty record
- `sys_admin`, `firstlogin_user`, `inactive_user` — no Faculty record (employee_type='regular_non_teaching')

Final user-employment-type matrix populated at Phase 1.

---

## Phase plan

| Phase | Topic | Risk |
|-------|-------|------|
| 0 | Orient: M10.md skeleton + CLAUDE.md status-line update + self-audit | low |
| 1 | TD-068 seed.py idempotency fix + Faculty + 5 sub-models + Alembic migration + designation expansion (4 → 7) + seed user backfill | medium |
| 2 | Faculty repository + service + FACULTY permission delta + permission seed extension | low |
| 3 | M7 engine Part A: structured channel_definition_json migration + ApprovalProcess admin UI rewrite (back-compatible with existing 4 processes; STEP-A pause before merge) | **HIGH** |
| 4 | M7 engine Part B: runtime fork resolution + decide-and-forward action + submit-time requestor picker + conditional first-stage resolver `dept_head_at_requestor_campus` (STEP-A pause before merge) | **HIGH** |
| 5 | FacultyRequest parametric model + 10 ApprovalProcess seeds with structured channels | medium |
| 6 | FileAsset image MIME extension + Faculty photo upload + FacultyDocument pattern wiring | low |
| 7 | Faculty self-service profile UI `/faculty/profile` (multi-section) + Faculty admin page `/admin/faculty` | medium |
| 8 | Faculty browse `/faculty` + Faculty requests page `/faculty/requests` (uses Phase 4 picker) | medium |
| 9 | E-019 campus-scoped Director routing (engine + matrix) + NonRegularFaculty contract-term expansion | medium |
| 10 | E-021 HoD recommend-via FACULTY leave + matrix YAML rewrite + Prof-tier opt-in checkbox + TD-038/TD-039 absorption (STEP-A pause before merge — touches shipped M8) | **HIGH** |
| 11 | Placeholder backfill migration (5 tables) + Faculty pickers in M5b assignment pages | medium |
| 12 | Bulk faculty CSV import (M5b pattern) | low |
| 13 | Dashboard widget + audit verification + TD-066/TD-067 cleanup + docs sweep | low |
| 14 | E2E + gate ritual + merge | low |

STEP-A discipline at Phases 3, 4, 10: prove the mechanism in isolation on a throwaway process or test matrix row before wiring into existing live processes. Bala pauses for explicit go-ahead between the prove-it commit and the wire-it commit.

---

## Gate criteria (§12 + standard)

- A faculty user (`employee_type='regular_teaching'`) can fully populate their record across all sections (identity-read-only, contact, photo, external IDs, education, experience, expertise, documents, sensitive PAN/Aadhaar).
- A faculty user can raise an FDP_REQUEST that traverses the configured channel through approver decisions including an OR-set pick at the {DIRECTOR, DEAN} stage.
- Leave requests by Asst Prof L10 designation route through HoD first, then matrix continues per §11.10.
- Leave requests by Prof designation with the opt-in checkbox ticked prepend a HoD stage; without the checkbox, route per matrix unchanged.
- Bulk import of 20 synthetic faculty rows succeeds (M5b two-stage validate→commit pattern).
- Placeholder backfill migration round-trips clean; all existing M5b-seeded assignments preserved with real Faculty FK references; rollback path verified.
- Existing 4 ApprovalProcess seeds (CPC_FUND_RELEASE, NRF_APPROVAL, DSW_CLEARANCE, LEAVE_APPROVAL) continue to operate unchanged after engine extension; all M7/M8 existing tests stay green.
- TD-068 regression test: seed runs twice on fresh DB; user count identical after second run.
- Standard gate ritual: 3× deterministic non-E2E runs, fresh-clone from origin (`~/clone-verify/`), E2E by Bala (`DURGAM_E2E=1 uv run pytest tests/e2e/ -q --no-cov`), manual walkthroughs per role, incognito checks per new page.

---

## Phase commit timeline

*(To be filled during implementation.)*

| Phase | Topic | SHA(s) | Net new tests | Notes |
|-------|-------|--------|---------------|-------|
| 0 | Orient: M10.md skeleton + CLAUDE.md update + self-audit | `0e3c0e4` | 0 | Phase 0 orient |
| 1A | Faculty models + migration + IQAC_OFFICE role + TD-068 regression test | `7548086` | 9 | Additive only; no designation expansion or seed backfill |
| 1B | Designation taxonomy expansion (4→7) + seed user backfill + TD-069 | `d64ffe6` | 10 | Migration cb2de963f0b8; 39 failed 1363 passed ×3; honest deviation: PRASANTHI→PSN campus code |
| 2 | Repos + service + permission catalog + TD-070 | `5cfbac6477818cde67f2c2826d9dd946ac4a0cb7` | 46 | Additive only; no migration; 39 failed 1409 passed x3; recovered post-parallel-pytest contamination + HR_OFFICE + test_seed assertion fixes |
| 3A | Approval engine OR-set schema + resolver registry (STEP-A) | `c1ff933b0194a64adb197861a6d1a02271dc05c8` | 18 | Migration 4bcc3e4671b8; 39 failed 1427 passed ×3; honest deviations: (approval_process_id,stage_index) FK pair, pick_mode on process not per-option |
| 3B | TD-071 resolver fix + get_stage_pick_mode helper | `aa6f52a58a542c16f07fa9fba43d1b653e686de5` | 5 | 39 failed 1432 passed x3; 4 ground-truth integration tests dropped during recovery -> TD-072 |
| 4 | resolve_stage_authority engine flow helper (STEP-A) | `232250c89f90a22e6fe25a153d620a6cfe840b1f` | 8 | Additive only; no schema, no wiring; pick_mode runtime consumption |
| 5A | FacultyRequest model + repo + service skeleton | `0d863610f01c752881bbd844ff449bc0dd39405c` | 24 | Additive; no engine wiring; no approval submission; request_type VARCHAR for extensibility |
| 5B | faculty_noc seed + picked_option_ids_json + submit_for_approval | `f85f2bb44c5728401e8e9164676eebb42f7f582c` | 15 | Migration e5d2a1c8f094; seeded_session READ-ONLY seed tests; Stage 1 one ApprovalStageOption (HoD→AhoD fallback internal to resolver) |
| 5C1 | OR-set engine eligibility + approve_request + TD-073/074 | `4d2a578e58e3e70fe5126d8a3e78b22da1fb3c0b` | 11 | Engine: _resolve_or_set_approvers added; _resolve_approvers OR-set-first; 30 existing engine unit tests unchanged. Service: StageAlreadyAdvancedError, UnauthorizedActorError, approve_request(). OR-set HoD+AhoD integration tests verify resolver wiring end-to-end; legacy M7 backward-compat test passes. TD-073 (double-query) + TD-074 (service→service) filed. |
| 5C2 | reject_request + withdraw_request + TD-075/076 | `7b568bf` | 15 | Service: InvalidRejectReasonError, UnauthorizedWithdrawError, reject_request() (delegates engine.reject; maps "not an approver" → UnauthorizedActorError), withdraw_request() (faculty.user_id ownership guard; delegates engine.withdraw). TD-075: seeded_session.flush hygiene (Open). TD-076: OR-set semantics RESOLVED at 4d2a578 (informational). Smoke: 4 passed, 11 skipped (skips expected in isolated run; seeded OR-set tests run in full suite). |
