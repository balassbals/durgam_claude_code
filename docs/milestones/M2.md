# M2 — Admin Module

**RFP reference:** §12, M2 row.  
**Gate clause:** "System Admin can construct any role and verify scoped permissions."

---

## Inherited from M1

### State-layer unit test coverage — resolved at M2

**Decision (M2):** Unit-test non-trivial state handlers via service-layer simulation.
Do NOT attempt to construct live Reflex state objects in unit tests — Reflex's
`__setattr__` requires parent state initialization that only works with a running
Reflex server.

**Non-trivial handlers at M2 (unit-tested in `tests/unit/test_admin_states.py`):**
- `AdminUsersState.create_user` — form validation, service call, temp-password display
- `AdminUsersState.reset_user_password` — temp password generation, one-time display
- `BulkImportState.upload_csv` — CSV parsing, validation branching, multiple state vars

**Thin handlers (E2E-covered only):** `load_users`, `load_roles`, `soft_delete_user`,
`hard_delete_user`, `create_role`, `save_role_permissions`, `commit_import`.
These delegate directly to a service with at most one state var mutation.

**Forward to M3:** This two-tier approach (service-layer simulation for non-trivial;
E2E for thin) is the M2 pattern. M3 should apply the same classification to its
handlers. If M3 introduces handlers with complex branching (e.g. leave-rule edge
cases in the Leave module), they warrant service-layer unit tests. Thin handlers
remain E2E-only.

---

## M2 scope (all shipped)

**(a)** User CRUD (create, list, search, edit, soft-delete, hard-delete) with role assignment.  
**(b)** Role CRUD with permission assignment.  
**(c)** Permission matrix UI (resource-first accordion on role detail; `/admin/permissions` read-only listing).  
**(d)** Bulk import — CSV for users only (two-stage: validate → preview → commit).  
**(e)** Password auto-gen + email on user creation (`must_change_password=True`).  
**(f)** User edit/delete email (role-change and deactivation notifications).  
**(f2)** Admin-initiated password reset email (admin resets user password from user detail).  
**(g)** Basic-user fallback — `BASIC_USER` role (level=1); all users receive it at creation.

---

## Bug fixed at M2: can() scope_id discrimination

`can()` in `durgam/auth/permissions.py` had a latent bug: a `UserRole` scoped to a
specific `scope_id=X` incorrectly returned `True` when the check passed `scope_id=None`.
The guard was `if user_role.scope_id is not None and scope_id is not None` — the second
condition was wrong. Fixed to: `if user_role.scope_id is not None: if scope_id is None or mismatch: continue`.

Five discrimination cases are verified in `tests/unit/test_permissions.py`:
- Matching scope_id → granted ✓
- Different scope_id → denied ✓
- scope_id=None in check → denied ✓ (the fixed bug)
- UserRole with scope_id=None (all departments) → granted for any scope_id ✓
- Global role (scope_type=None, scope='*') → granted always ✓

---

## Design decisions established at M2

**(i) Permission matrix UI:** Resource-first accordion. See `durgam/pages/admin/role_detail.py`.

**(ii) Admin landing:** Stats panel + navigation tiles at `/admin`.

**(iii) Responsive table (Tier 1):** Card layout below 768px for lookup/management tables.
Documented in CLAUDE.md under "Patterns established at M2."

**(iv) Bulk import:** Two-stage flow; partial success (valid rows commit, invalid skip).
`BulkImportService.validate_user_csv()` + `commit_user_import()`.

**(v) Nav registry:** Config-dict pattern; each module's `__init__.py` calls `register(NavEntry(...))`.
`BaseState._load_nav_entries()` caches filtered list at login. Documented in CLAUDE.md.

**(vi) State-layer tests:** Non-trivial handlers unit-tested via service simulation (see above).

---

## Hard-delete audit policy (plan refinement 1)

`UserAdminService.hard_delete_user()` counts audit rows before deleting. If count > 0,
raises `HardDeleteBlockedError`. The auditlog table is INSERT+SELECT only — no UPDATE
available to null out `actor_user_id`. This is the correct policy: preserve audit integrity
by blocking hard-delete of users with history.

Users with no audit rows (typically freshly-created test/ephemeral users) can be
hard-deleted after soft-delete.

---

## Permissions are seed-only (plan refinement 3)

Permission (resource, action, scope) triples are defined exclusively in `scripts/seed.py`.
The `/admin/permissions` page is read-only. No create/update/delete form exists.

M2 permission seed: 13 triples covering `system:manage:*`, `user:read/write/delete:*`,
`role:read/write/delete:*`, `permission:read:*`, `academic_year:read/write:*`,
`department:read:department`, `leave_request:approve:department`, `audit_log:read:*`.

Future milestones extend the seed when they introduce new resources.

---

## Gate checklist

### Functional gate: "System Admin can construct any role and verify scoped permissions"

| Step | Description | Status |
|---|---|---|
| Login as sys_admin via UI only (not URL) | Navigate from / to /admin | ⬜ |
| Create new user | Temp password shown, email in Mailpit | ⬜ |
| New user first login | must_change_password forced flow | ⬜ |
| Admin password reset | New temp password shown and emailed | ⬜ |
| Construct new role | Code, name, level set; saved | ⬜ |
| Assign scoped permission to role | department:read:department on TEST_HOD | ⬜ |
| Assign role to user with scope_id | UserRole(scope_type=department, scope_id=X) | ⬜ |
| Verify via permission check widget | ✓ for scope_id=X; ✗ for Y and None | ⬜ |
| Soft-delete then hard-delete | Deactivation email; blocked if audit rows | ⬜ |
| Bulk import 10-row CSV, 2 errors | Preview; error report; commit 8 valid | ⬜ |
| Basic-user redirect | student_001 → / with flash (no Admin nav link) | ⬜ |

### UX gate (per gate_verification.md + UX Charter)

| Check | Status |
|---|---|
| Three-width (360/768/1280px) on all admin pages | ⬜ |
| Nav shell: institution name, username, logout, footer | ⬜ |
| All admin pages reachable from / by clicking | ⬜ |
| Destructive actions show confirmation dialogs | ⬜ |
| List pages have loading/empty/error states | ⬜ |
| No hex literals in new component files | ⬜ |
| All form fields labelled | ⬜ |

### Automated gate

```bash
uv run alembic upgrade head          # apply full_name migration (f64d89eff350)
uv run python scripts/seed.py        # seed 20 permissions, BASIC_USER role, etc.
uv run pytest tests/unit/ tests/integration/ tests/property/ -q
# → 171 passed ✅

DURGAM_E2E=1 uv run pytest tests/e2e/ -q --no-cov   # run 1
DURGAM_E2E=1 uv run pytest tests/e2e/ -q --no-cov   # run 2
DURGAM_E2E=1 uv run pytest tests/e2e/ -q --no-cov   # run 3
# → 27 pass + 3 skip (home_loads tests) each run, identical counts required
```

**Non-E2E suite: 171/171 ✅** (confirmed programmatically 2026-05-16)

### Seeded-user pristine check

After E2E runs:
```sql
SELECT username, failed_login_count, locked_until, is_deleted, must_change_password
FROM users WHERE username IN ('sys_admin','dean_sci','firstlogin_user','inactive_user','student_001')
ORDER BY username;
```
All five rows must match seed values.

---

## Resume notes (2026-05-16 — implementation complete)

**Implementation status: COMPLETE.** All code changes merged to branch `m2-admin`.

**What's left for close-out:**
1. Administrator runs fresh-clone gate verification per `docs/prompts/gate_verification.md`.
2. Manual incognito verification: /admin and /admin/* redirect with no chrome flash at any network speed.
3. Permission accordion: dean_sci shows exactly 4 checkboxes checked; BASIC_USER shows 0; sys_admin shows 20.
4. Permission check widget: resource → action filtering works; values clear on navigation.
5. All five seeded users pristine after 3 E2E runs.
6. Update gate checklist checkboxes above to ✅ as each is verified.
7. Merge: `git checkout main && git merge --no-ff m2-admin -m "Merge M2: Admin Module" && git tag -a M2-admin`.
8. Update `CLAUDE.md` "Current milestone" to M3 in the merge commit.

**Next session:** Write E2E suite, then documentation, then run gate verification.

**Files modified in M2:**
- `durgam/auth/permissions.py` — scope_id discrimination bug fix
- `durgam/nav/registry.py` — new
- `durgam/states/base.py` — nav caching added
- `durgam/states/admin_*.py` — new (4 files)
- `durgam/pages/admin/` — new (7 files)
- `durgam/pages/shared/` — new (3 files)
- `durgam/pages/components.py` — nav shell upgraded
- `durgam/services/user_admin.py`, `role_admin.py`, `bulk_import.py` — new
- `durgam/services/password.py` — generate_temp_password added
- `durgam/notifications/admin_emails.py` — new
- `durgam/repositories/role.py`, `permission.py`, `user_role.py` — new
- `durgam/repositories/user.py` — extended + inherits BaseRepository
- `scripts/seed.py` — BASIC_USER role + M2 permissions
- `durgam/durgam.py` — admin routes registered
- `tests/unit/` — 6 new test files
- `tests/integration/` — 3 new test files
- `tests/property/` — 1 new test file
