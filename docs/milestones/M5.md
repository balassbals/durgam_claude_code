# M5 — Configuration — Identity Attachments

**RFP reference:** §9.3, §12 M5
**Branch:** TBD (M5a was `m5a-config-assets`)
**Split:** M5a (File Infrastructure + Identity Assets) / M5b (Assignments & Approval Config)

M5a gate passed. See `docs/milestones/M5a.md` for session log.

---

## M5b — Assignments & Approval Config

### Scope

1. MentalHealthCounsellor roster (AY-scoped, Director letterhead DOCX export)
2. FacultyMentorAssignment (model + thin UI; rich UI at M14)
3. ClassTeacherAssignment (model + thin UI; rich UI at M14)
4. ClassCoordinatorAssignment (model + thin UI; rich UI at M14)
5. VisitingFaculty (E-003: external personnel per department)
6. NonOwnedCourse (course shared across departments)
7. UGTimetable (first/second year, Director's office)
8. ApprovalProcess + CPC + PurchaseProcedureRule config
9. Bulk import (CSV/Excel for users, faculty, students, courses, programs)

### M5 Inheritance Note (from M3)

ClassTeacher, ClassCoordinator, and FacultyMentor assignments reference
Faculty (M10) and Student (M12), which arrive after M5b.

**Option A confirmed:** Ship the assignment data model + schema at M5b; leave
UI as a thin management scaffold; implement full UI at M14 (Department module)
when both Faculty and Student exist.

### Sessions

See `docs/milestones/M5b.md` for the full session log (8 sessions).
