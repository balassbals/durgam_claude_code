INHERITED FROM M0: re-verify decorator contract under real session.

CONSIDER: add a ruff or mypy custom rule to enforce sa_type=_TIMESTAMPTZ
on datetime fields — prevents silent regression to TIMESTAMP WITHOUT TIME ZONE
when new fields are added without the pattern documented in CLAUDE.md.
